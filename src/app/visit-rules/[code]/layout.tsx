import React from 'react';

import Layout from '@/components/PageLayout';

export default function VisitRulesLayout({ children }: { children: React.ReactNode }) {
    return <Layout>{children}</Layout>;
}
